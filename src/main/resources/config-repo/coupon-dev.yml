server:
  port: 19099

spring:
  application:
    name: coupon
  config:
    import:
      - classpath:properties/datasource.yml
      - classpath:properties/jpa.yml
  jpa:
    properties:
      hibernate:
        default_schema: coupons