server:
  port: 19097

spring:
  application:
    name: review
  config:
    import:
      - classpath:properties/datasource.yml
      - classpath:properties/jpa.yml
  jpa:
    properties:
      hibernate:
        default_schema: reviews